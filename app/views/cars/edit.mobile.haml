= jqm_header do
  %h1 Edit car
  = link_to 'Delete', @car, :method => :delete, :confirm => 'Are you sure?', :"data-icon" => "delete", :class => "ui-btn-right"

= jqm_content do
  = render 'form', :car => @car